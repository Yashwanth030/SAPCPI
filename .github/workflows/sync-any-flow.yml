name: Sync Any iFlow

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    env:
      CPI_HOST: ${{ secrets.Host }}
      CPI_USERNAME: ${{ secrets.Client_ID }}
      CPI_PASSWORD: ${{ secrets.Client_Secret }}

    steps:
      - uses: actions/checkout@v4

      - name: Sync iFlow
        run: |
          echo "Connecting to $CPI_HOST"
